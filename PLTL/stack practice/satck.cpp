#include <stack>
#include <iostream>
using namespace std;
int main(){
    stack <int>value;
    for(int i = 0; i < 11; i++){
        value.push(i);
    }
    value.();
    
    cout << "Enter a number to remove";
}